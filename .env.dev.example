# Copie para um arquivo sem ".example" (ex: ".env.dev") ou exporte como variáveis de ambiente.
#
# Banco (Postgres)
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/planejamento?sslmode=disable
SPRING_DATASOURCE_USERNAME=planejamento
SPRING_DATASOURCE_PASSWORD=planejamento

# O app força sslmode=require via application.properties;
# sobrescrevemos aqui para o Postgres local (dev) aceitar sem SSL.
SPRING_DATASOURCE_HIKARI_DATA_SOURCE_PROPERTIES_SSLMODE=disable

# Necessário para o Spring (placeholder em application.properties)
SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT=org.hibernate.dialect.PostgreSQLDialect

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# RabbitMQ
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=planejamento
RABBITMQ_PASS=planejamento

# JWT issuer (precisa apontar para seu servidor de auth/Keycloak/etc.)
JWT_ISSUER=http://localhost:8080/realms/seu-realm

